import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      light: '#fff',
      main: '#272643',
      dark: '#2c698d',
      contrastText: '#fff',
    },
    secondary: {
      light: '#fff',
      main: '#272643',
      dark: '#2c698d',
      contrastText: '#fff',
    }
  }
});

export default theme;
